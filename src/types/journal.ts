export type JournalParams_Type = {
	contextId: number;
	title: string;
	body: string;
};

export type JournalData_Type = {
	id: number;
	title: string;
	body: string;
	createdAt: string;
	updatedAt: string;
};
